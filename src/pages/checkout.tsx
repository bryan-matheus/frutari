import React from 'react';

/**
 * Displays the checkout page.
 *
 * @return {React.ReactElement} Checkout page.
 */
export default function Checkout(): React.ReactElement {
  return (
    <div>
      <h1>Checkout</h1>
    </div>
  );
}
